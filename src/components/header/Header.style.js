import styled from 'styled-components'

const Image = styled.img`
    width: 115px;
`;

const Header = styled.header`
    position: fixed;
    background: white;
    left: 0;
    right: 0;
    z-index: 999;
`;

export default {
    Image,
    Header,
};
